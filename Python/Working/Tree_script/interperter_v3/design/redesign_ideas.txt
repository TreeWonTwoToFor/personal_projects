array, or a list needs to be implemented
    I think a list will be eaiser from a python perspective
1. The user should be able to make a list, and assign it to a variable
2. the user should be able to read an element, and have it go into the global variable
3. the user can write an element into a specific index
4. they need to be able to access the length (if it's a list, then python handles that well).

1. LIST name -> creates a list under that variable name
2. READ name INDEX -> grab the element of list 'name' from the index 'INDEX' and put it into ANS
3. WRITE name INDEX VALUE -> puts the value into the list at the given index
4. LEN name -> puts the length of the list into the ANS variable.
